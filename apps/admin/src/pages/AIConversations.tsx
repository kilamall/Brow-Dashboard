import React from 'react';
import AIConversationManager from '../components/AIConversationManager';

const AIConversations: React.FC = () => {
  return (
    <div className="p-6">
      <AIConversationManager />
    </div>
  );
};

export default AIConversations;
